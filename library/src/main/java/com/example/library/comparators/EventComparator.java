package com.example.library.comparators;

import android.app.usage.UsageEvents;

import java.util.Comparator;

/**
 * Created by Eric1 on 20/04/2017.
 */

/*public class EventComparator implements Comparator<UsageEvents.Event> {
    @Override
    public int compare(UsageEvents.Event lhs, UsageEvents.Event rhs) {
        return lhs.getTimeInMillis() < rhs.getTimeInMillis() ? -1 : lhs.getTimeInMillis() == rhs.getTimeInMillis() ? 0 : 1;
    }
}*/
